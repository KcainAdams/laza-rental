import React from 'react'
import ReactDOM from 'react-dom/client'
import LazaRental from './LazaRental'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LazaRental />
  </React.StrictMode>
)
